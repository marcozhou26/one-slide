# Marimekko 模块

适用：3–6 个细分同时表达横向规模占比和内部 2–5 类构成。

必需输入：细分名称、规模占比、绝对规模、增速、各构成份额与来源。细分宽度合计 100%，每列构成合计 100%。正文把 `CAGR` 改写成“年均增长”；CAGR 的正式名称、公式和原始字段写入 PowerPoint 备注。页底只显示一行数据来源，合成披露并入同一行。

阻塞：任一层不对平时 `MEKKO_RECONCILIATION_FAIL`。禁止用补差项偷平、按视觉调整真实面积或编写主战场结论。
